#include<stdio.h>
void main()
{
    char ch = 'A';
    if (ch == 'a'|| ch == 'A' || ch == 'e' || ch == 'E' || ch == 'i' || ch == 'I' || ch == 'o' || ch == 'O'|| ch =='u' || ch == 'U' )
    printf("the character %c is vowel",ch);
    else 
    printf("the character %c is consonent",ch);
}    